// Test setup file
// Set NODE_ENV to test to ensure any environment-specific behavior
process.env.NODE_ENV = 'test'

// Mock console.log to reduce noise during tests (optional)
// console.log = jest.fn()